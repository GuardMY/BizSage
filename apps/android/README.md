# BizSage Android

Android is intentionally deferred beyond V1.

V1 keeps backend contracts compatible with a future Android client, but only the
Web app is implemented. The first Android version should reuse the same auth,
conversation, message streaming, intelligence, and source-display APIs.
