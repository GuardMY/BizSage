spring:
  flyway:
    enabled: false
  cache:
    type: simple
  sql:
    init:
      mode: always
      schema-locations:
        - classpath:db/migration/h2/V1__baseline.sql
        - classpath:db/migration/h2/V2__learning_diagnosis_guided_workflow.sql

bizsage:
  jwt:
    secret: test-jwt-secret-0123456789abcdef
  privacy:
    key: 12345678901234567890123456789012
  admin:
    collection:
      scheduler-enabled: false

COLLECTOR_URL: embedded://collector
