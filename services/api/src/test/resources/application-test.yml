spring:
  datasource:
    url: jdbc:h2:mem:bizsage_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-test.sql

bizsage:
  admin:
    collection:
      scheduler-enabled: false

COLLECTOR_URL: embedded://collector
