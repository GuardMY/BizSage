server:
  port: ${API_PORT:8080}

spring:
  application:
    name: bizsage-api

bizsage:
  jwt:
    secret: ${JWT_SECRET:change-me-v1-dev-secret-change-me}
  privacy:
    key: ${DATA_ENCRYPTION_KEY:0123456789abcdef0123456789abcdef}
